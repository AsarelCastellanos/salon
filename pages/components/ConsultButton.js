import React from "react";
import { PopupButton } from "@typeform/embed-react";

const ConsultButton = (props) => {
  return (
    <PopupButton id="BThDIw3h" type="button" className={props.style}>
      Book a Consult
    </PopupButton>
  );
};

export default ConsultButton;
